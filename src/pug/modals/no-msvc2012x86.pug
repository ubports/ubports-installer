#no-msvc2012x86-modal.modal.fade(tabindex='-1', role='dialog')
  .modal-dialog(role='document')
    .modal-content
      .modal-header
        button.close(type='button', data-dismiss='modal', aria-label='Close')
          span(aria-hidden='true') ×
        h4.modal-title Missing Dependencies
      .modal-body
        p
          | Your computer is missing the Visual C++ 2012 32-bit libraries. The installer will be unable to detect or flash Samsung devices.
        p
          | Please download and install vcredist_x86.exe from #[a(onclick="shell.openExternal('https://www.microsoft.com/en-us/download/details.aspx?id=30679')") Microsoft's download page], then click "Try again" to restart the installer.
        p
          | If you will not be installing on a Samsung device, you can continue without Samsung device support.
      .modal-footer
        button.btn.btn-default(type='button', data-dismiss='modal') Continue without Samsung device support
        button.btn.btn-primary(type='button', data-dismiss='modal', onclick="ipcRenderer.send('restart');") Try again
  script.
    ipcRenderer.on("user:no-msvc2012x86", () => {
      modals.show('no-msvc2012x86');
    });
